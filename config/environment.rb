require 'bundler/setup'
Bundler.require(:default, :development)

# In the example above, we're first requiring 'bundler/setup'.
# If we don't do this, our app won't know to use bundler to install our gems.
# Without that line, our Gemfile becomes pointless.
