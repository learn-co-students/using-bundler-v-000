#we're first requiring 'bundler/setup'
require 'bundler/setup'
#two arguments that you are passing into the .require method must be passed in the correct order, shown above.
Bundler.require(:default, :development)
